# Web Framework (for MT5 Bridge)
flask>=2.0.0
flask-cors>=4.0.0
pyzmq>=25.0.0

# HTTP Client
requests>=2.28.0

# YAML Parser
pyyaml>=6.0

# LLM Client (Optional - Phase 7)
# Only required if ZAI_API_KEY is set
anthropic>=0.40.0